[package]
name = "fast-vrifa-cuda"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
anyhow.workspace = true
bytemuck.workspace = true
cudarc = { version = "0.19.4", default-features = false, features = ["std", "driver", "nvrtc", "dynamic-loading", "cuda-12040"] }
fast-vrifa-core = { path = "../fast-vrifa-core" }
libloading.workspace = true
ndarray.workspace = true
opencv = { workspace = true, features = ["imgproc"] }
vrifa-core.workspace = true

[dev-dependencies]
image = "0.25"

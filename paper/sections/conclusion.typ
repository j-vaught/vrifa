= Conclusion

The integrated pipeline reaches mask IoU $X.X X X$ and boundary $F_1$ $Y.Y Y Y$ across a $55$-frame hand-labeled subset spanning eleven distinct VARTM infusion runs. A per-sample component-removal ablation on the same subset reports the marginal IoU effect of disabling each named primitive in turn, namely the peak-brightness reference, the darken-only delta, the region-of-interest restriction, the dynamic-lag reference selection, the morphological cleanup, and the temporal lock. The effect of any one primitive varies by sample. No single primitive dominates every infusion regime in the subset, and several primitives are neutral or counterproductive on at least one sample, which is the empirical content of the per-sample breakdown rather than a defect of the integrated configuration. The pipeline runs at $30$ frames per second on a single CPU. The empirical contribution is the joint configuration together with the per-sample evidence that the components in use are doing different work in different regimes.


= Related Work

This work builds upon four research pillars of the composite-manufacturing and computer-vision literatures. The first pillar is the forward and inverse physical modeling of resin flow in Liquid Composite Molding (LCM) and Vacuum-Assisted Resin Transfer Molding (VARTM), which predicts the per-pixel front from a permeability field or recovers a permeability field from observed front geometry. The second pillar is the embedded-sensor observation of the front during infusion, which produces dense in-plane wetness signals at an instrumentation cost that scales with sensor density. The third pillar is the real-time control of resin infusion, where the front signal drives valves, vents, or pumps, and where a recent camera-based subset replaces point sensors with computer vision. The fourth pillar is the classical computer-vision tradition of thresholding, morphology, image alignment, and topological border-following, whose named operators the integrated pipeline composes into a per-frame per-pixel mask producer. The integrated pipeline sits at the seam of these four pillars, because the per-pixel mask is the artifact that the first pillar currently obtains from simulation, the second pillar approximates with sensors, the third pillar produces with a segmenter it does not characterize per component, and the fourth pillar supplies the named operators from which the mask is composed. The remainder of this section addresses each pillar in turn, narrows to the camera-based subset of the third pillar because that subset is the closest prior art to the work reported here, and finally attributes the operators of the fourth pillar to their canonical sources and names the specific evidence gap that this paper supplies.

== Modeling, sensing, and control of the flow front

The modeling literature treats the flow front as a given input. Forward Darcy-flow simulators couple a depth-averaged Darcy solver to a level-set front with high-permeability-medium layers and progressive-saturation effects @Adhikari2021HPMLevelSet, and end-to-end VARTM simulation with reinforcement compaction and post-filling pressure relaxation has been validated experimentally for plate and stiffened-plate geometries @Govignon2008FullField. Inverse formulations work in the other direction. Comas-Cardona and co-authors recover an in-plane permeability field by coupling optical areal-weight measurement with central-injection front observation @ComasCardona2014OpticalInverse, and Di Fratta, Klunker, and Ermanni invert pressure-sensor traces against an analytic front model to update injection parameters online @DiFratta2016PressureInversion. Learning-based variants extend the same thread. Park and co-authors couple electromechanical PZT signals to a tree-plus-GAN model that both identifies the live front and forecasts its near-future position @Park2025FlowFrontGAN, and physics-informed neural networks invert permeability fields against synthetic Darcy benchmarks @Mao2026PINNInversion. A parallel surrogate-modeling thread learns the flow-front image from sparse pressure sensors on simulated injection runs at time-step accuracy near 92 percent @Stieber2020FlowFrontNet, and extends to material-property inference under simulation-to-real transfer with reported IoU near $0.50$ @Stieber2023SimToReal. The dependency this paragraph has in common is the front map itself. The modeling family currently obtains that map from finite-element simulation or from manual annotation of plate experiments, and the integrated pipeline of this paper produces it directly from production video, which is the input these methods would need to fine-tune on real runs rather than to remain in simulation.

The observation literature measures the front directly but does so through instrumentation that discretizes it. Konstantopoulos and co-authors survey the in-line sensing landscape and group the field around dielectric, thermal, ultrasonic, fiber-optic, and electrical-resistance modalities @Konstantopoulos2014InlineSensing. Dielectric tracking by Yenilmez, Sozer, and others gives unsaturated and saturated front positions at each electrode pair, at an installation cost that is hard to beat, but the spatial resolution of the resulting front is bounded by the electrode pitch @Tifkitsis2014Dielectric. Matsuzaki and co-workers dramatically improve coverage with an area-sensor array that tiles a polyimide film into a dense capacitive matrix and recovers the impregnated-area ratio per cell, which is the closest sensor-side analogue to a full-field flow-front mask @Matsuzaki2011AreaSensor. The resulting front is, however, still a quantized grid that is destructive to instrument inside production tooling and that adds permeability to the stack-up. Fiber-optic strategies use Fiber Bragg Grating arrays or Optical Frequency Domain Reflectometry to time-stamp resin arrival at every grating along an embedded fiber, with recent distributed-sensing implementations pushing toward life-cycle structural-health monitoring of large Carbon-Fiber-Reinforced-Polymer (CFRP) parts @Matsuzaki2022FiberOptic. These methods deliver excellent temporal precision and can survive infusion and cure, but the same coupling between sensor density and front fidelity reappears, since arbitrary front shapes between gratings are interpolated rather than measured. Distributed carbon-nanotube textile sensors push the spatial-coverage axis further by integrating the sensing layer into the lay-up itself @DaiThostenson2020CNTTextile. The limitation common to this paragraph is that front fidelity is bounded by sensor pitch, and the methods that approach pixel-level coverage do so by adding instrumentation to the part they measure.

The control literature uses the front signal to act on the process in real time. Lawrence and co-workers couple a vector of point-sensor arrival times to a finite-element flow model so that multi-segment injection valves can be re-sequenced to chase the front toward starved regions and away from race-tracks @Lawrence2005ClosedLoop, and the broader closed-loop and sensor-driven control thread for resin transfer molding has been developed for decades @SozerBickertonAdvani2000OnLine @NielsenPitchumani2002ClosedLoop @HsiaoAdvani2004FlowSensingI @Devillard2005FlowSensingII @LawrenceFriedAdvani2005AuxiliaryGates @MinaieChen2005Adaptive. A more recent camera-based subset replaces point sensors with a top-mounted camera, segments the front from the live video stream with a classical pipeline of difference-and-morphology operators, and toggles inlet valves on the resulting front position @LekanidisVosniakos2020IJMMS @AlmazanLazaro2018Materials @AlmazanLazaro2022JMP. Esposito and co-workers fuse a top-mounted camera with dielectric channels and use a learned You-Only-Look-Once (YOLO) detector trained on labeled video as the segmenter @Esposito2025AIFusion. The common pattern across the control family is that the controller is downstream of a segmenter that is reported only end-to-end. The control loop reports filling-time accuracy or part-quality outcomes rather than per-component segmentation behavior, and the prior camera-based pipelines treat the segmentation step as a unit that succeeds or fails as a whole. Because the camera-CV subset of this paragraph is the closest prior art to the integrated pipeline reported here, the next subsection unpacks it in more detail.

Adjacent to these three pillars is a post-cure non-destructive-evaluation literature and a closed-loop digital-twin literature, both of which the integrated pipeline does not build on directly but which sit naturally downstream of it. Defect characterization in cured composite parts reads the trace of the front after the fact. Ultrasonic C-scan recovers delamination, voids, and porosity at sub-millimeter resolution @WronkowiczKatunin2022UltrasonicNDE, infrared thermography covers larger areas at lower spatial resolution and without contact, and fusion of C-scan with thermographic phase imaging measurably improves shallow-defect signal-to-noise on CFRP laminates @Cao2024UltrasoundThermographyFusion. The defects these methods detect are the consequence of a particular flow trajectory, and the per-frame mask produced by the integrated pipeline describes that trajectory directly while it is still acting. A complementary digital-twin thread fuses in-situ sensor data, forward simulation, and learned controllers to identify defect signatures in real time and to use model-predictive control over vacuum and thermal cycles to suppress dry-spot formation @Jha2026DigitalTwin. Such systems are candidate consumers of the per-pixel front signal at the sensing layer, since the wet-versus-dry observable they require is exactly what the pipeline emits.

== Camera-based front segmentation

The closest prior art to the integrated pipeline is the camera-CV subset of the control family above, and the contribution of this paper is best located against it. Lekanidis and Vosniakos built a webcam-plus-microcontroller rig for Vacuum-Assisted Resin Infusion (VARI) that segments the resin flow front with a Matlab pipeline of Gaussian blur, grayscale conversion, contrast stretch, Otsu binarization, closing on a ten-pixel disk, Sobel edge detection, opening with a one-hundred-twenty-pixel area threshold, and dilation, and toggles inlet valves on the resulting front position @LekanidisVosniakos2020IJMMS. Almazán-Lázaro and co-workers close a similar loop on resin infusion, regulating flow-front velocity to an experimentally-determined optimum with a Proportional-Integral-Derivative (PID) controller over a pipeline that combines region-of-interest cropping, Scaramuzza-style lens-distortion correction, histogram equalization, first-frame absolute differencing, grayscale conversion, a five-by-five mean filter, Sobel-gradient segmentation, paired erosion and dilation, and small-area removal @AlmazanLazaro2018Materials @AlmazanLazaro2022JMP. Esposito and co-workers replace the classical segmenter with a learned YOLO detector trained on labeled video, fuse the output with dielectric channels for redundancy, and demonstrate real-time front monitoring and prediction on a single-fabric study @Esposito2025AIFusion. The three systems demonstrate that vision alone, or vision plus dielectric, can drive VARTM monitoring and control on the bench at which they were instrumented.

What the three systems do not report is the question this paper answers. They report end-to-end behavior, never the joint or individual contribution of the segmentation primitives in use. They treat the per-frame mask producer as a unit that succeeds or fails as a whole rather than as a composition of named operators whose marginal contributions can be measured. They disagree on details that propagate directly into how robust each pipeline is to disturbance. The Almazán-Lázaro pipeline clips the difference so that only darkening counts as wetting and the Lekanidis-Vosniakos pipeline does not. The Lekanidis-Vosniakos pipeline tracks a per-pixel running maximum as the reference and the Almazán-Lázaro pipeline pins a single early frame as a first-frame absolute differencing reference. The Esposito pipeline imposes a learned temporal regularization through the YOLO detector trained on sequential frames, and the two classical pipelines operate frame-by-frame on the segmentation side. None of these disagreements has been adjudicated on a labeled multi-mold benchmark because no such benchmark exists in the published literature. Each prior paper reports excellent agreement on the single mold it was developed for, and the reader is left to guess which knob settings would carry over to a different bench.

Adjacent to the front-segmentation literature is a broader composite-defect-detection literature that increasingly leans on deep convolutional and transformer detectors trained on hand-curated defect crops. The material-defect-detection survey by Ren and co-workers identifies pixel-accurate labels as the primary bottleneck across that survey, ahead of model capacity @Ren2024MaterialDefectSurvey. Modern detectors such as YOLO @Redmon2016YOLO @Jocher2023YOLOv8 and segmentation architectures such as U-Net @Ronneberger2015UNet illustrate why this matters, because they are starved for in-domain pixel labels rather than for parameters. A per-pixel front-mask producer that runs deterministically on production video, as opposed to a learned detector that requires its own labeled-video training set per bench, is therefore upstream of that bottleneck. The integrated pipeline of this paper is positioned to fill that upstream role, and the 55-frame hand-labeled benchmark introduced alongside it is positioned to support evaluation of any future learned segmenter that wants to claim improvement over the classical baseline.

Two specific operators define the difference between the prior camera-CV pipelines and the integrated pipeline. The lens-distortion calibration step that Almazán-Lázaro and co-workers apply once at setup time @Scaramuzza2006Toolbox is the only operation in either prior camera-CV pipeline that has no analogue in this work, and the run-time camera-shift registration described in Section~3 is the only operation in this work that has no analogue in either prior camera-CV pipeline. The two are complementary axes rather than substitutes, and a deployed system would benefit from both.

== Classical computer-vision primitives

The integrated pipeline stays deliberately within well-understood classical operators because the empirical claim of this paper is per-component, and a learned segmenter would conflate the contributions of the components inside it. Every named stage maps to a primitive with a canonical attribution and a documented behavior, and the per-component ablation reported in Section~5 is well defined precisely because each stage can be disabled independently. Wet-versus-dry separation in each frame is performed by Otsu's between-class-variance threshold @Otsu1979Threshold, with the Triangle method of Zack and co-workers @Zack1977Triangle exposed as an alternative when one class dominates the histogram. Camera-shift registration combines phase correlation @KuglinHines1975PhaseCorrelation on a Hanning-windowed downsample with an iterative refinement based on the Enhanced Correlation Coefficient maximization of Evangelidis and Psarakis @EvangelidisPsarakis2008ECC, run on a static-edge mask of the current region of interest so the warp is driven by mold and frame edges that do not move with the wet front. Polygon export to Common Objects in Context (COCO) and YOLO formats uses the Suzuki-Abe topological-border-following algorithm @Suzuki1985Border, optionally simplified by Douglas-Peucker reduction at a user-supplied tolerance @DouglasPeucker1973. The peak-brightness reference, the darken-only clip, the post-delta blur, the paired close-and-open morphology with an area-floor connected-components filter, and the count-based temporal lock are per-pixel compositions of arithmetic and comparison whose value lies in the joint configuration in which they appear rather than in any single canonical attribution.

None of these primitives is novel in isolation. The contribution of this paper is the joint composition, the per-stage parameterization, and the per-component evidence that the joint matters on a labeled multi-mold subset. The four pillars on which this work builds each touch the flow front from a distinct angle, and none on its own supplies the evidence this paper provides. The modeling pillar consumes a front map it does not measure, and obtains the map from simulation. The observation pillar measures a front it cannot densely cover, and bounds front fidelity by sensor pitch. The control pillar acts on a front it produces with a segmenter it does not characterize per component. The classical computer-vision pillar supplies the named primitives but does not on its own evaluate any composition of them on a multi-mold VARTM benchmark. The camera-CV subsystems closest to this work, Lekanidis-Vosniakos and Almazán-Lázaro, differ in which subset of the classical primitives they use and report only end-to-end control behavior on a single mold. None reports the joint contribution of the components in use, none reports the marginal cost of any component left out, and none evaluates against a labeled video benchmark that spans more than one mold. This paper supplies that evidence on a 55-frame hand-labeled subset spanning eleven distinct infusion runs, with the component-removal ablation reported both as an eleven-sample mean and as a per-sample breakdown.
